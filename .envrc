# automatically activate dev environment when in project tree using direnv.
ACT=.venv/benchmarks/bin/activate

if [ -e $ACT ]
then
    source $ACT
    echo "Activated local environment using ${ACT}"
else
    echo "Activation script ${ACT} not found for local environment."
fi
