product {
			1..25	BigTable bigTables
			1..25	SmallTable smallTables
			num	1-500	totalPlaces
			}
			structure BigTable {
				3..5 Chair chairs
			}
			structure SmallTable {
				1..2 Chair chairs
			}
			structure Chair {
			}

			behavior {
				require totalPlaces = 100

				imply totalPlaces = count(bigTables.chairs) + count(smallTables.chairs)
			}
